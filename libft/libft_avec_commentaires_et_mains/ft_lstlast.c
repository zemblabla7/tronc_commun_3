#include "libft.h"

t_list *ft_lstlast(t_list *lst)
{
    t_list *content;

    content = t_list -> first
    While (*content != NULL)
    {
        content = content -> next;
    }
    return(content);
}
